# AI maintenance note:
# Keep all Belgium source definitions in this file.
# Put long portal lists or buyer lists in config/source_lists/ and reference them by path.
# Do not add speculative enabled sources without verifying the official entry point first.
sources:
  - source_id: belgium_public_procurement
    name: Belgian eProcurement Public Portal
    kind: official_procurement
    jurisdiction: belgium
    base_url: https://int.publicprocurement.be/
    collector: disabled_scaffold
    scan_method: api
    extractor: generic_listing_html
    source_tier: primary
    priority: 0.74
    enabled: false
    regions:
      - europe
      - belgium
    languages:
      - nl
      - fr
      - de
      - en
    notes: Official Belgian eProcurement portal. Left disabled until the public token plus trace-header API flow is implemented and verified as stable for worker ingestion.
