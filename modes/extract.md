# Extract Mode

Goal: convert a single notice page or dossier into a normalized professional opportunity record.

Workflow:

1. read the raw source page or brief
2. map explicit facts to the procurement-aware schema
3. mark uncertain fields in `evidence_note`
4. avoid filling missing values by inference
5. capture implementation path and qualification signals before producing prose
6. write the structured record before producing prose
